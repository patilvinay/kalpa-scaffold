const assert = require('assert');
const kalpaScaffold = require('../index.js');

describe('kalpaScaffold', () => {
  it('has a test', () => {
    assert(false, 'kalpaScaffold should have a test');
  });
});
